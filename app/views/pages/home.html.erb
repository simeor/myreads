<div class="container">
  <div class="home-books">
    <h1>Pages#home</h1>

    <ul class="list-group">
      <% @top_books.each do |book| %>
        <li class="list-group-item"><%= book.title %> (<%= book.author %>)</li>
      <% end %>
    </ul>
  </div>
</div>
